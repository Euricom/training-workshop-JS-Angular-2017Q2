// named export
export const MAX_LENGTH = 1000;

// named export
export class Car {
    constructor(private name: String) {
    }
}

// default export
const config = {
    name: 'hello',
};

export default config;
